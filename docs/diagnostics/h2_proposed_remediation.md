# H2 / G2 KB Remediation Proposal

Generated by `/tmp/h2_migrate.py` against `references/case-studies/peer-review-kb.json`.

**This script is read-only. No KB modification has occurred.**

## Summary

- Total concerns currently tagged with `seed_stability_gate`: **52**
- Proposed re-tag (drop seed_stability_gate, add code-availability gates): **26**
- Keep (genuine seed-variance signal): **1**
- Ambiguous (human review required): **25**

## Proposed re-tags (26)

### PR-010 / PR-010-C05

- **category**: `reproducibility`
- **tags**: `['feature_engineering_unclear', 'irreproducible_methods', 'aggregation_functions']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'feature_engineering_audit_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'feature_engineering_audit_gate', 'reporting_bias_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\birreproducib']
- **concern_text**: 'It is not clear how features were actually merged from 24-hour epochs. How were values combined — average, range, median, mean? It would not be possible to reproduce these models a'

### PR-013 / PR-013-C03

- **category**: `reproducibility`
- **tags**: `['irreproducible_methods', 'statistical_model_missing', 'rate_calculation_unclear']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'reporting_bias_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\birreproducib']
- **concern_text**: 'From the methods it is unclear how the rate of decline was calculated. There are no longitudinal models reported. It looks like per-patient regression was fitted and slope extracte'

### PR-015 / PR-015-C02

- **category**: `reproducibility`
- **tags**: `['reproducibility', 'code_availability', 'data_availability', 'synthetic_data_sharing']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bdata (is )?(not )?(un)?available\\b', '\\brepository\\b']
- **concern_text**: 'My fundamental objection to nearly all synthetic data papers: Is the real input data available? Is the synthetic data available? Where is the public code repository? They cannot an'

### PR-017 / PR-017-C04

- **category**: `reproducibility`
- **tags**: `['incomplete_code', 'competitor_suboptimal_settings', 'fair_comparison']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcode (is )?(not )?(public|public(ly)?|shared|released|posted)\\b']
- **concern_text**: 'Analysis code is publicly shared but I cannot find the code corresponding to LDpred2 analyses. I am surprised LDpred2 performs consistently worse than lassosum2 — maybe consider ex'

### PR-024 / PR-024-C07

- **category**: `reproducibility`
- **tags**: `['coefficients_not_provided', 'model_transparency']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'reporting_bias_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcoefficients?\\b']
- **concern_text**: 'Model coefficients and variables need to be clearly provided to allow further validation.'

### PR-030 / PR-030-C04

- **category**: `reproducibility`
- **tags**: `['code_as_pdf', 'github_needed', 'reproducibility']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bgithub\\b']
- **concern_text**: 'Great that ML pipeline is available, but code is hard to read from a PDF. Please upload to GitHub.'

### PR-034 / PR-034-C04

- **category**: `reproducibility`
- **tags**: `['model_availability', 'reasonable_request_only', 'open_access']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\brepository\\b']
- **concern_text**: 'The model should be uploaded to an open-access repository, not only available on reasonable request, as this limits reproducibility.'

### PR-052 / PR-052-C01

- **category**: `reproducibility`
- **tags**: `['architecture_unclear', 'hyperparameters_missing', 'irreproducible_methods']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'model_selection_audit_gate', 'reporting_bias_gate', 'prediction_replay_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'model_selection_audit_gate', 'reporting_bias_gate', 'prediction_replay_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\birreproducib']
- **concern_text**: 'Section on Model Development is unclear and poorly explained. Many details missing regarding architecture and optimization. No hyperparameter tuning described. No schematic figure '

### PR-062 / PR-062-C01

- **category**: `reproducibility`
- **tags**: `['training_details_missing', 'loss_function_missing', 'preprocessing_missing', 'extreme_ensemble', 'ml_contest_approach']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'reporting_bias_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\btraining details?\\b']
- **concern_text**: 'DL model description is vague: loss function completely missing, training details (optimizer, dev split, epochs) absent, image preprocessing completely missing (DICOM→PNG, dimensio'

### PR-066 / PR-066-C05

- **category**: `reproducibility`
- **tags**: `['weights_not_shared', 'partial_reproducibility']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['execution_attestation_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcode (is )?(not )?(un)?available\\b', '\\brepository\\b', '\\bweights?\\b.*\\b(not |un)?(provided|available|released|shared)']
- **concern_text**: "Code available but trained weights not shared. Checked code in repository — seems reasonable but can't run without pre-trained model."

### PR-102 / PR-102-C04

- **category**: `reproducibility`
- **tags**: `['no_code_availability', 'conflict_of_interest', 'reproducibility']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcode (is )?(not )?(un)?available\\b']
- **concern_text**: 'Code not available for review; preprocessing pipelines of proprietary MLCS framework not disclosed; conflict of interest from Univfy raises reproducibility concerns.'

### PR-105 / PR-105-C03

- **category**: `reproducibility`
- **tags**: `['coefficients_not_provided', 'model_transparency', 'no_code_availability']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcoefficients?\\b']
- **concern_text**: 'Model coefficients not published; code only available under academic license from Oxford University Innovations, hindering independent validation.'

### PR-106 / PR-106-C06

- **category**: `reproducibility`
- **tags**: `['reproducibility', 'training_details_missing', 'hyperparameters_missing']`
- **current_gates**: `['reporting_bias_gate', 'seed_stability_gate', 'execution_attestation_gate', 'model_selection_audit_gate']`
- **proposed_gates**: `['reporting_bias_gate', 'execution_attestation_gate', 'model_selection_audit_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\brepository\\b', '\\bgithub\\b']
- **concern_text**: 'GitHub repository README lacks software/hardware requirements and dependencies; hyperparameter selection approach (systematic or ad-hoc) not documented.'

### PR-107 / PR-107-C05

- **category**: `reproducibility`
- **tags**: `['hyperparameter_reporting', 'irreproducible_methods']`
- **current_gates**: `['reporting_bias_gate', 'seed_stability_gate', 'execution_attestation_gate', 'model_selection_audit_gate']`
- **proposed_gates**: `['reporting_bias_gate', 'execution_attestation_gate', 'model_selection_audit_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\birreproducib']
- **concern_text**: 'Chosen hyperparameters (embedding dimensions, pooling ratio) not stated in manuscript, limiting reproducibility of the GNN tuning.'

### PR-109 / PR-109-C04

- **category**: `reproducibility`
- **tags**: `['hyperparameter_reporting', 'no_code_availability', 'irreproducible_methods']`
- **current_gates**: `['reporting_bias_gate', 'seed_stability_gate', 'execution_attestation_gate', 'model_selection_audit_gate']`
- **proposed_gates**: `['reporting_bias_gate', 'execution_attestation_gate', 'model_selection_audit_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\birreproducib']
- **concern_text**: 'Tuning-parameter settings, software versions for LDpred2/lassosum/PRS-CS not documented; no code provided for baseline PRS estimation step.'

### PR-111 / PR-111-C02

- **category**: `reporting`
- **tags**: `['cv_strategy_unspecified', 'irreproducible_methods', 'model_description_unclear', 'no_code_availability']`
- **current_gates**: `['reporting_bias_gate', 'feature_engineering_audit_gate', 'seed_stability_gate', 'execution_attestation_gate', 'model_selection_audit_gate', 'split_protocol_gate']`
- **proposed_gates**: `['reporting_bias_gate', 'feature_engineering_audit_gate', 'execution_attestation_gate', 'model_selection_audit_gate', 'split_protocol_gate', 'publication_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bhyperparameters?\\b.*\\b(not |un)?(reported|provided|specified|disclosed)', '\\birreproducib']
- **concern_text**: "LASSO cross-validation type, hyperparameters, and software details not specified; combinatorial 'linear' models were actually logistic regression, and scikit-learn call / CV scheme"

### PR-RO-07 / PR-RO-07-C09

- **category**: `reproducibility`
- **tags**: `['code_unavailable_at_review', 'reproducibility', 'deferred_release']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcode (is )?(not )?(un)?available\\b']
- **concern_text**: 'The authors stated that they will only make the code available upon publication (p. 25) so I was unable to review the code.'

### PR-EXP-0084 / PR-EXP-0084-C06

- **category**: `reproducibility`
- **tags**: `['code_unavailable', 'irreproducible_methods']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\birreproducib']
- **concern_text**: 'Code availability: Given the lack of detail in the description of the methods and no code availability it is impossible to reproduce the presented results of the manuscript, even i'

### PR-EXP-0085 / PR-EXP-0085-C05

- **category**: `reproducibility`
- **tags**: `['reproducibility_documentation', 'tool_versioning', 'containerization', 'input_output_specification']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bgithub\\b']
- **concern_text**: 'Many of the tools do not have version numbers, which are necessary for reproducibility. The GitHub for AMR-GNN lacks information about what the model uses to predict and what outpu'

### PR-EXP-0097 / PR-EXP-0097-C07

- **category**: `reproducibility`
- **tags**: `['code_repo_404', 'deferred_release', 'code_unverified', 'repo_visibility']`
- **current_gates**: `['publication_gate', 'execution_attestation_gate', 'seed_stability_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\brepository\\b']
- **concern_text**: "The link to the Bitbucket repository isn't working. Without access to the repo, verifying any of the author's claims is impossible. Therefore, assessing the usability of your softw"

### PR-EXP-0101 / PR-EXP-0101-C01

- **category**: `reproducibility`
- **tags**: `['code_unavailable_at_submission', 'github_404', 'post_revision_release']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcode (is )?(not )?(un)?available\\b', '\\bgithub\\b']
- **concern_text**: 'How do the authors plan to visualize the KG and deliver to the users? Currently, the github is not available. The code is not available at the github site.'

### PR-EXP-0119 / PR-EXP-0119-C09

- **category**: `reproducibility`
- **tags**: `['reproducibility', 'installation_failure', 'demo_broken']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\brepository\\b', '\\bgithub\\b']
- **concern_text**: 'Note on reproducibility: We attempted to run the code provided in the GitHub repository but encountered some issues during the installation and in the demo.py file.'

### PR-EXP-0149 / PR-EXP-0149-C04

- **category**: `reproducibility`
- **tags**: `['broken_code_link', 'repository_access']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bgithub\\b']
- **concern_text**: 'Code availability: the Github link does not work.'

### PR-EXP-0150 / PR-EXP-0150-C05

- **category**: `reproducibility`
- **tags**: `['statistical_methods_undocumented', 'code_availability_missing', 'ethics_statement_missing']`
- **current_gates**: `['publication_gate', 'execution_attestation_gate', 'seed_stability_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bgithub\\b']
- **concern_text**: 'There is no section that summarizes the statistical methods used in this manuscript. There is no statement on whether the underlying code can be accessed (i.e., within a GitHub rep'

### PR-EXP-0157 / PR-EXP-0157-C02

- **category**: `reproducibility`
- **tags**: `['partial_code_release', 'github_link_provided', 'infrastructure_dependency']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bgithub\\b', '\\bdoi\\b']
- **concern_text**: 'A major point is that the source codes do not seem to be available. This is a must and the authors should provide a github link or even better, a DOI, to their codes.'

### PR-EXP-0191 / PR-EXP-0191-C04

- **category**: `reproducibility`
- **tags**: `['code_unavailable_initially', 'reproducibility_resolved', 'github_release']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **proposed_gates**: `['publication_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **action**: re-tag (drop seed_stability_gate)
- **availability_hits**: ['\\bcode (is )?(not )?(un)?available\\b']
- **concern_text**: 'Very important: there is no code available to assess the reproducibility of the work. The code should be made available to the reviewer with a well detailed README file.'

## Keep as-is (1)

### PR-EXP-0200 / PR-EXP-0200-C04

- **category**: `evaluation_metrics`
- **tags**: `['missing_confidence_intervals', 'seed_variance_unreported', 'single_run_results']`
- **current_gates**: `['evaluation_quality_gate', 'ci_matrix_gate', 'seed_stability_gate', 'prediction_replay_gate']`
- **action**: keep (genuine seed-variance signal)
- **seed_hits**: ['\\bseed\\b', '\\brandom[ _-]?seed\\b']
- **concern_text**: 'No error bars and confidence intervals are provided. It is standard practice to go train a network several times to determine the influence of the random seed used for training. Wi'

## Ambiguous — human review (25)

### PR-003 / PR-003-C09

- **category**: `reproducibility`
- **tags**: `['no_code_availability', 'reproducibility']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'I did not find the file with code.'

### PR-003 / PR-003-C10

- **category**: `reproducibility`
- **tags**: `['no_code_availability', 'reproducibility']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: "I may have missed something, but I didn't see a link to the code."

### PR-005 / PR-005-C01

- **category**: `study_design`
- **tags**: `['imaging_protocol_unclear', 'data_acquisition', 'reproducibility']`
- **current_gates**: `['cohort_definition_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'A lot more detail needs to be provided on which parts of the CT images were used and correlated to histology images. Which cut of CT was used and why? Were they sagittal or cross-s'

### PR-005 / PR-005-C07

- **category**: `reproducibility`
- **tags**: `['reproducibility', 'insufficient_methods_detail', 'data_sharing']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'There is a general lack of details (data more than code) for the work to be reproduced.'

### PR-007 / PR-007-C05

- **category**: `reproducibility`
- **tags**: `['reproducibility', 'hitl_protocol_unclear', 'methods_insufficient']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'The unusual nature of the training set (nearly 50% malignant) and the lack of detail about how human input was provided makes it impossible to reproduce the work.'

### PR-055 / PR-055-C04

- **category**: `reproducibility`
- **tags**: `['privacy_details_missing', 'pseudo_code_needed', 'fl_protocol']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'More details needed to evaluate privacy properties. Should include pseudo-code showing exactly what information is transmitted between sites.'

### PR-070 / PR-070-C02

- **category**: `reproducibility`
- **tags**: `['abstract_only_insufficient', 'code_not_open_source']`
- **current_gates**: `['seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'How can agent reliably create calculation functions from paper abstracts only? Calculation details are in the main body. Toolset creation pipeline not open-sourced.'

### PR-EXP-0084 / PR-EXP-0084-C04

- **category**: `model_selection`
- **tags**: `['reproducibility_methods_underspecified', 'hyperparameter_search_undefined', 'cv_protocol_missing']`
- **current_gates**: `['model_selection_audit_gate', 'tuning_leakage_gate', 'seed_stability_gate', 'execution_attestation_gate', 'prediction_replay_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'There is a lack of detail in the description of the methods to a point where reproducing the experiments are impossible. The number of cross validation folds is not specified, and '

### PR-EXP-0085 / PR-EXP-0085-C09

- **category**: `reproducibility`
- **tags**: `['pipeline_documentation_gap', 'input_preparation_unclear', 'docker_dependency', 'deployability_constraint']`
- **current_gates**: `['publication_gate', 'execution_attestation_gate', 'seed_stability_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'Although the figures illustrate that the features are mainly derived from unitigs, SNPs, and FCGR, the description in the Methods section and the demo dataset provided suggest that'

### PR-EXP-0086 / PR-EXP-0086-C09

- **category**: `reproducibility`
- **tags**: `['hyperparameter_reporting', 'compute_specifications_missing', 'training_curves_unreported']`
- **current_gates**: `['model_selection_audit_gate', 'publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'The supplementary materials lack detailed information regarding model training processes, specifically describing hyperparameter tuning procedures, parameter selection justificatio'

### PR-EXP-0092 / PR-EXP-0092-C06

- **category**: `reproducibility`
- **tags**: `['code_availability', 'simulated_dataset_release']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'I did not see a link to the code. Apologies if I missed this.'

### PR-EXP-0096 / PR-EXP-0096-C04

- **category**: `reporting`
- **tags**: `['inconsistent_reporting', 'seed_aggregation_method', 'table_text_discrepancy']`
- **current_gates**: `['reporting_bias_gate', 'seed_stability_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'Inconsistencies in Reporting: In line 142, the percentage of children with recommendations (27.8%) differs from the value reported in Table 1. Similar discrepancies are noted for o'

### PR-EXP-0101 / PR-EXP-0101-C04

- **category**: `reproducibility`
- **tags**: `['closed_source_llm', 'kg_reproducibility', 'third_party_dependency']`
- **current_gates**: `['publication_gate', 'reporting_bias_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'Use of ChatGPT: While ChatGPT demonstrates impressive performance across many benchmarks, the choice of a closed, proprietary platform for constructing a shared knowledge graph is '

### PR-EXP-0103 / PR-EXP-0103-C07

- **category**: `reproducibility`
- **tags**: `['missing_data_files', 'incomplete_artifact_release', 'post_revision_completion']`
- **current_gates**: `['publication_gate', 'missingness_policy_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'Codes and data files were available through figshare and code ocean but not completely with many missing data files (e.g. DILImap_deseq2.h5ad). Please ensure all data files are pro'

### PR-EXP-0105 / PR-EXP-0105-C02

- **category**: `reproducibility`
- **tags**: `['code_unavailable', 'validation_data_inaccessible', 'simulated_data_request']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate', 'reporting_bias_gate', 'prediction_replay_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: "The study's biggest limitation is lack of reproducibility. Without data and source code, the value of this study is significantly diminished and evaluation of the methodology is im"

### PR-EXP-0111 / PR-EXP-0111-C01

- **category**: `reproducibility`
- **tags**: `['llm_reproducibility', 'prompt_consistency', 'stochasticity_assessment']`
- **current_gates**: `['execution_attestation_gate', 'publication_gate', 'seed_stability_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: "It was not entirely clear if the revision addressed the prior reviewer comments about whether multiple repeat prompts were attempted to asses for consistency in ChatGPT ADA's behav"

### PR-EXP-0111 / PR-EXP-0111-C03

- **category**: `reproducibility`
- **tags**: `['chat_log_legibility', 'text_format_provided', 'replication_blueprint']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'As a more general presentation comment, many of the chat interaction screenshots (e.g. Figure 2) appear illegible at lower resolution when formatted into PDF. These need to be high'

### PR-EXP-0112 / PR-EXP-0112-C07

- **category**: `reproducibility`
- **tags**: `['code_availability_initial_refusal', 'open_sourced_after_pressure', 'patent_review']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'Will any code for SUDO be made publicly available?'

### PR-EXP-0119 / PR-EXP-0119-C03

- **category**: `evaluation_metrics`
- **tags**: `['evaluator_blinding', 'reader_study_protocol', 'evaluation_reproducibility']`
- **current_gates**: `['evaluation_quality_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'Regarding the evaluations, how many certified dermatologists were involved in the evaluation and comparison process? What type of evaluation was conducted? Were the evaluators awar'

### PR-EXP-0147 / PR-EXP-0147-C02

- **category**: `reproducibility`
- **tags**: `['preprocessing_undocumented', 'normalization_unclear', 'reproducibility']`
- **current_gates**: `['feature_engineering_audit_gate', 'publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: "However, a detailed description of MS raw data treatment is missing, e.g., what was used to normalize the data? What was the reproducibility of the signals' measurement? The raw da"

### PR-EXP-0148 / PR-EXP-0148-C02

- **category**: `reproducibility`
- **tags**: `['code_availability_missing', 'commercial_vs_research', 'reproducibility']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'A strong limitation is that the source codes are not being made available. Of course, this is understandable if what is being tested here is a commercial product. In that case, how'

### PR-EXP-0155 / PR-EXP-0155-C06

- **category**: `reproducibility`
- **tags**: `['code_availability_missing', 'architecture_reproducibility']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'The code does not seem to be made public, and the details of the architecture are not enough to allow anyone to re-code and re-produce it potentially. The methodology seems to be i'

### PR-EXP-0187 / PR-EXP-0187-C03

- **category**: `reproducibility`
- **tags**: `['data_not_public', 'reproducibility_limited', 'privacy_constraints']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'The only contribution is the dataset, but it will not be publicly available.'

### PR-EXP-0192 / PR-EXP-0192-C01

- **category**: `external_validation`
- **tags**: `['clustering_reproducibility', 'cross_dataset_validation', 'harmonization', 'adni_subsets']`
- **current_gates**: `['external_validation_gate', 'distribution_generalization_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'The clustering results are not externally validated. At the very least it would be beneficial to show the same clustering results on ADNI1 and ADNI2 separately.'

### PR-EXP-0194 / PR-EXP-0194-C03

- **category**: `reproducibility`
- **tags**: `['partial_data_sharing', 'code_released', 'raw_data_restricted', 'hgrac_regulation']`
- **current_gates**: `['publication_gate', 'seed_stability_gate', 'execution_attestation_gate']`
- **action**: ambiguous — needs human review
- **concern_text**: 'It is not clear if the authors are sharing code and data ensuring reproducible research. The authors should share all code and data that reproduces figures and key survival analysi'
